{
  "name": "roll",
  "description": "Roll dice using RPG dice notation. Supports XdY, modifiers (+/-), keep/drop highest/lowest (kh/kl/dh/dl), exploding (!), compound (!!), penetrating (!p), rerolls (r/ro), target success/failure counting, and math expressions. Returns the per-die results and the total. Use this for any dice roll — never fabricate a result.",
  "inputSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "notation": {
        "type": "string",
        "description": "Dice notation. Examples: '4d6+2', '2d20kh1' (advantage), '2d20kl1' (disadvantage), '4d6kh3' (ability score), 'd20!' (exploding crit), '8d6'."
      }
    },
    "required": [
      "notation"
    ]
  }
}